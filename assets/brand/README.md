# 브랜드 에셋 슬롯

인트로(스플래시) 화면이 참조하는 로고 자리.

| 용도 | 파일명 | 비고 |
|---|---|---|
| Eduino AI 로고 | `eduino_ai_logo.png` | 인트로 화면 중앙. 배경 투명 또는 밝은 배경 권장(인트로는 라이트 배경). |

- 파일이 없으면 인트로는 텍스트 워드마크("Eduino AI" + 태그라인)로 폴백한다.
- GitHub 웹에서 올리는 법: 저장소 → `assets/brand/` 폴더 → **Add file → Upload files** → `eduino_ai_logo.png` 업로드 → Commit.
